# Check In

## 开发环境

安装依赖并为 Electron 编译 `better-sqlite3`：

```bash
pnpm electron:setup
```

之后启动开发环境：

```bash
pnpm dev
```

如果出现 `NODE_MODULE_VERSION` 不匹配，单独重新编译原生模块：

```bash
pnpm electron:rebuild
```

## 活动提示

打开



## 代码统计

开始日期

当月