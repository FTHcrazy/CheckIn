# CheckIn 小说编辑器 —— 产品规划（PRD v0.7）

> 状态：草案待评审（v0.7 步骤二落地版）｜ 作者：产品通 ｜ 日期：2026-09-21
> 一句话定位：**一款专业、主流形态的本地小说编辑器，码字体验是第一优先级——任何功能若影响输入流畅度，一票否决。**
>
> **变更记录**：
> - v0.1 定位"写作伴侣"，手稿编辑器列为非目标；
> - v0.2 按产品决策升级为**专业小说编辑器**，原四大辅助模块（灵感/设定/大纲/检索）重排为 P1 支撑面板，编辑器核心上位 P0，载体仍为 WorkerWindow 窗口改造；
> - v0.3 增补 **R20 角色标记与悬浮资料卡**（正文人名 ↔ 设定卡联动：词库高亮 + hover 出卡），新增配套 Non-goal（不做 NLP 人名识别）。
> - v0.4 按产品决策升级数据模型：设定卡升级为**通用要素实体**（人名/地名/等级/自定义类型 + JSON 扩展字段），新增**多态关联表**支撑要素互相关联，新增**等级体系**与**用户自定义等级转换关系**。数据层 M1 一次到位，UI 分期落地（R23–R27）。
> - v0.5（2026-09-20）**对照代码实现全量盘点**：新增 §6.5「实现现状盘点」（已实现 / 部分实现 / 未实现 / 代码有但文档未体现四张对照表）；确认当前形态为**无书架、打开即编辑器**（作品切换在顶栏下拉，补 R29 作品管理）；**R27 升级为「一键生成人物关系网络」并给出框架设计**（数据已由 R24 备齐）；**R18 随机起名扩展为完整框架**；**新增 R28 地图创建**（架空世界地图编辑器，Non-goal 明确排除真实地理数据）；补录 R30 伏笔与章节梗概（代码已实现、文档缺位）；§9 增补**分步实施路线**。
> - v0.6（2026-09-20）**步骤一「补齐 M1 收尾」落地并验收通过**（对应应用 v1.9.0–v1.9.2）：R5 设置持久化、R6 续写位置记忆（作品/章节/光标/滚动四元组原位恢复）、R29 作品 CRUD（含级联删除）、R10 章节内查找替换全部完成；§6.5 盘点表同步刷新（R1/R5/R6/R10/R29 移入"已实现"），§9 里程碑与实施路线标记步骤一 ✅。附带体验修复：章节删除（含快照级联 + 悬浮删除按钮）、卷级「+」新建章节入口、点击正文下方空白可聚焦续写、未命名卷展示为「第N卷」。
> - v0.7（2026-09-21）**步骤二「M2 剩余面板能力」落地并验收通过**（对应应用 v1.9.3）：R25 等级体系管理（体系/等级项 CRUD + EntityDetail「当前境界」绑定，复用 novel_links 多态关联）、R13 TXT 导出（整本/分卷/分章，章节标题按序号设置派生与界面所见一致；设定卡导出做实）、R14 usage_log 埋点（四事件 + 今日统计改净增口径）、R23 自定义类型管理（自建类型 + Context 运行期派生 meta + 标注层内联注色）；§6.5.2「部分实现」清零，§9 步骤二标 ✅。**M2 功能面至此全部收口**。

---

## 1. 市场基准与定位（为什么这样切）

主流竞品格局（2026-09 调研）：

| 阵营 | 代表 | 主打 | 短板 |
|------|------|------|------|
| 平台官方工具 | 作家助手(起点)、番茄作家助手、七猫助手 | 免费、AI 内嵌、一键发布、收益数据 | 被单一平台绑定，数据在平台侧 |
| 独立码字工具 | 小黑屋（强制码字/敏感词）、墨者（多端同步/历史版本）、大神码字（买断/默认不上云）、橙瓜（素材库全家桶） | 存稿安全、专注码字、隐私 | 会员费/广告、云依赖或功能臃肿 |

**主流编辑器的标配能力**（缺一个就不"专业"）：章节/分卷树、防丢稿（自动保存+历史版本）、实时字数统计与目标、专注模式、素材库（人物/大纲/灵感）、敏感词检测、导入导出。

**我们的切入点**：作者对"数据主权"是真实诉求（大神码字把"默认不上云"当核心卖点）。CheckIn 本地 SQLite 单机架构天然契合 → **做一款本地优先、轻量秒开、防丢稿做到极致的纯码字编辑器**。差异化亮点之一：**设定库与正文实时打通**（R20 人名高亮 + 悬浮资料卡）——竞品的设定库大多是"写完切过去查"的孤岛，我们让它长在正文里。

## 2. 码字体验设计规范（本 PRD 的核心章节 = 工程验收标准）

| 维度 | 硬指标 / 设计约定 |
|------|-------------------|
| 输入延迟 | 按键到上屏 P95 < 50ms；**中文 IME 组合输入（拼音候选期间）零掉帧、零丢字**——第一优先验收项 |
| 保存策略 | 输入停顿 800ms 防抖落库 + 失焦/关窗/每 30s 强制 flush；全程无"保存中"阻塞感 |
| 防丢稿三保险 | ① 防抖自动保存；② 增量快照（每 5 分钟或每 500 字，每章保留最近 20 版，可预览回滚）；③ 启动时崩溃恢复检查（检测未落库快照→提示还原） |
| 零打断原则 | 永不弹模态对话框（含关闭确认）；目标达成为轻提示；**移除现有 Esc 关窗行为**（编辑场景 Esc 属高频误触） |
| 中文排版默认值 | 正文区宽度 ~42em 居中、行高 1.9、段间距 0.6em、首行缩进两格（可关）、亮/暗/护眼底色三主题（复用现有 ThemeProvider） |
| 统计常驻 | 底部状态条：本章字数 / 今日新增 / 码字速度(字/分) / 今日目标进度条；字数口径与主流平台一致（含标点字符数，可切换纯汉字） |
| 续写无感 | 记忆上次作品/章节/光标偏移/滚动位置，打开即回到原位续写 |
| 核心快捷键 | Ctrl+S 手动保存、Ctrl+P 章节快速跳转（模糊搜索）、Ctrl+Enter 新章节、F11 专注模式（完整清单 P1） |
| **正文标注层**（v0.3） | 角色名（含别名）基于**设定卡词库白名单**高亮；仅可视区增量匹配、防抖 300ms；IME 组合期间挂起；标注层为统一基建（M2 承载人名高亮，P2 复用于敏感词标黄），单次输入的开销预算 < 2ms |
| **悬浮资料卡**（v0.3） | 悬停高亮人名 200ms 后浮现**无焦点 popover**（移开 100ms 消失，防扫过闪烁）；键盘输入/滚动时立即关闭；永不抢焦点、不阻断输入、不劫持滚轮 |

## 3. 目标与非目标

### 目标

| # | 目标 | 衡量方式 |
|---|------|---------|
| G1 | 码字核心循环零摩擦：打开→定位到上次位置→继续写 ≤ 3 秒 | 启动+恢复计时抽测 |
| G2 | 丢稿事件 = 0，任意崩溃可回滚 | 防丢稿演练清单（杀进程/断电模拟）全过 |
| G3 | 章节管理达到主流专业形态（章节树/状态/排序/分卷） | 功能对照竞品标配清单 |
| G4 | 创作素材闭环服务于码字：卡壳时 3 秒内调出设定/大纲/灵感 | 右栏面板唤起即用 |
| G5 | 设定库随写作"自动生长"：正文中的人名可秒级标记、随时悬浮速查 | R20 联动链路验收 |

### 非目标（守住码字体验与单机定位）

| 非目标 | 原因 |
|--------|------|
| 富文本正文（粗体/图片/表格/批注） | 主流平台发布即纯文本；砍掉它 = 砍掉编辑器内核最大复杂度与性能风险。正文即纯文本段落流 |
| **NLP 中文人名自动识别**（v0.3 新增） | 通用分词/实体识别对中文人名误判率不可控，误判即污染正文标注、打断心流——违背零打断原则。人名来源只有两条可靠路径：设定卡词库（白名单匹配）+ 用户手动标记 |
| 云同步 / 多端 / 协作 | 单机产品，数据主权即卖点，过早 |
| AI 生成/润色/续写 | 2026 已是行业标配，但需 LLM 服务与密钥管理 → **独立立项**，不混入编辑器迭代 |
| 强制码字锁机（小黑屋式） | 负体验风险高，先观察真实需求（P3 评估） |
| 平台发布对接 | 平台 API 不开放、规则各异，远期再说 |

## 4. 用户故事

- 作为**网文作者**，我希望打开编辑器直接回到上次写的章节和光标位置，以便无缝续写。
- 作为**网文作者**，我希望断电/崩溃后一字不丢，并能回滚到任一历史快照，以便安心码字。
- 作为**网文作者**，我希望实时看到本章字数、今日新增和目标进度，以便保持日更节奏。
- 作为**网文作者**，我希望左侧章节树可新建/排序/标记状态（草稿/完稿），以便管理几十章的大部头。
- 作为**网文作者**，我希望写卡壳时右侧划出人物卡/大纲/灵感便签，以便不离开正文解决设定问题。
- 作为**网文作者**，我希望正文里写过的角色名自动高亮，鼠标移上去浮现 TA 的资料卡，以便写作中途秒查人设不出戏。
- 作为**网文作者**，我希望选中正文里的新名字一键"标记为角色"（无卡则秒建、有卡则关联为别名），以便设定库随写作自动长出来。
- 作为**网文作者**，我希望一键自动排版（段首缩进、全角标点统一），以便贴平台前不用手工清理。
- 作为**网文作者**，我希望按 Ctrl+P 模糊搜索章节名直接跳转，以便在百章大书里不迷路。
- 作为**网文作者**，我希望一键把已建好的人物关联渲染成**关系网络图**，以便一眼看清阵营与主线，写群像时不遗漏、不冲突。
- 作为**网文作者**，我希望卡名字时按风格**随机起一批名**并避开已用名，以便把新配角秒速落卡。
- 作为**网文作者**，我希望用轻量画布**画一张架空世界地图**（城池/山脉/关卡 + 连线标距离），以便设定地理格局并随手导出插图。
- 作为**网文作者**，我希望在顶栏直接新建/重命名/删除作品，以便多开几本书而不需要先经过一个书架页面。

## 5. 产品结构

```text
WorkerWindow（改造为编辑器窗口：默认 1200×760，min 800×560，记忆尺寸/位置）
├─ 左栏（可收起）：作品切换 + 卷/章节树（状态标记、排序）
├─ 中央：码字区 —— 纯文本、打字机/沉浸模式、底部常驻统计条
│          └─ 正文标注层：角色名高亮 → hover 悬浮资料卡
└─ 右栏（可收起）：支撑面板 Tab = 大纲 ｜ 要素库（含一键关系网络）｜ 灵感速记 ｜ 全库检索 ｜ 工具（起名 / 地图，v0.5）
```

三栏皆可收起 → 纯码字区一行 UI 都不多。默认打开状态：左栏展开、右栏收起。
**无书架页**：窗口打开即编辑器（首次自动建"未命名作品"，零配置开写）；作品管理收敛在顶栏下拉内嵌菜单（R29），完整书架页 P3 评估。

## 6. 需求清单

### P0 —— 编辑器核心（缺一不可，M1 范围）

| # | 需求 | 验收要点 |
|---|------|---------|
| R1 | 作品与章节管理 | 作品 CRUD；章节标题/状态(草稿/完稿)/排序(上下移)/每章字数；章节树左侧常驻 |
| R2 | 码字编辑器内核 | 纯文本段落流；IME 完全兼容（搜狗/微软拼音实测）；撤销/重做可靠；大章节(3 万字)滚动流畅 |
| R3 | 防丢稿体系 | §2 三保险全部落地；快照可列表预览、一键回滚；启动崩溃恢复提示 |
| R4 | 实时统计与目标 | 本章/今日字数、码字速度、今日目标设置与进度条；口径含标点可切换 |
| R5 | 排版与主题设置 | 三主题复用现有 ThemeProvider；字号/行距/首行缩进可调 |
| R6 | 续写位置记忆 | 作品/章节/光标/滚动位置持久化，重开窗口原位恢复 |

### P1 —— 创作支撑与专业形态（M2 范围）

| # | 需求 | 说明 |
|---|------|------|
| R7 | 右栏支撑面板 | 大纲板 / 要素库(角色/地点/势力/物品/等级体系/自定义类型) / 灵感速记 / 全库检索 四合一（v0.1 核心模块整体平移至此；要素模型见 R23–R25 与 §7） |
| R8 | 打字机模式 + 专注模式 | 当前行垂直居中；F11 隐藏全部 UI |
| R9 | 分卷管理 | 卷→章两级树，卷内拖拽排序 |
| R10 | 章节内查找替换 + 全书检索 | Ctrl+F/H；全书检索合并右栏检索入口 |
| R11 | 自动排版 | 段首缩进规范化、全角标点统一、多余空行清理 |
| R12 | 全局快捷键 + 托盘 | Alt+W 唤起（含冲突降级）；托盘菜单入口 |
| R13 | 导出 | 整本/按卷/按章导出 TXT；Markdown（P2） |
| R14 | 本地使用埋点 | usage_log 表：唤起/保存/快照/目标达成事件，支撑 §8 指标 |
| **R20** | **角色标记与悬浮资料卡**（v0.3） | ① **词库高亮**：要素（角色/地点，可按类型开关）的名称/别名自动构成白名单词库，正文实时高亮（性能预算见 §2）；② **悬浮资料卡**：hover 高亮人名 → 无焦点 popover 展示摘要（角色名+别名 / 性格标签 / 关系 / 一句话设定）+ "查看/编辑完整设定"入口，内容懒加载；③ **选区快速标记**：选中正文文字 → 右键/快捷键"标记为角色" → 无卡则秒建（名字带入）、有卡则选择"关联为别名"；④ 与 P2 敏感词检测共用统一标注层基建 |
| **R23** | **要素类型扩展与自定义字段**（v0.4） | 要素类型不写死：预置角色/地点/势力/物品/等级体系，支持用户自建类型；每类型在公共字段（名称/别名/摘要/内容）之外可添加自定义键值字段（如"金手指""战力""阵营"），数据层以 JSON 扩展列承载（见 §7） |
| **R24** | **要素互相关联**（v0.4） | 任意要素两两建关系（多态关联表）：师徒/亲属/敌对/所属/出身地/当前境界…关系类型预置 + 可自定义；卡面展示"关系列表"，双向可备注 |
| **R25** | **等级体系管理**（v0.4） | 等级体系（如"修真境界""武道九品"）= 有序等级项列表，顺序可调整；角色/势力可关联"当前等级"（即一种要素关联） |
| **R29** | **作品管理（书架轻量版，v0.5）** | 当前窗口**无书架页，一点开即编辑器**（零配置开写），作品切换仅靠顶栏下拉、且**无新建/重命名/删除作品能力**（代码盘点确认）。补齐：顶栏下拉内嵌"新建作品 / 重命名 / 删除（需确认，含关联数据级联）"，作品项展示字数与章节数；**完整书架页（封面墙/搜索/排序）列为 P3 评估项**，多作品重度用户出现前不投入 |

### P2 —— 专业进阶（M3 / 独立立项）

| # | 需求 | 说明 |
|---|------|------|
| R15 | 敏感词检测 | 本地词表实时标黄——**复用 R20 的统一标注层基建** |
| R16 | 码字统计报表 | 日/周字数曲线、写作日历热力图；可与 activities 写作时段打通 |
| R17 | AI 辅助 | 润色/续写/起名——**独立立项**，单独 PRD |
| **R18** | **随机起名工具**（v0.5 扩展框架） | **定位**：纯本地词库组合式随机起名，服务"卡名字"高频场景。**框架**：① **词库**——内置静态资源随应用分发（姓氏池 + 按风格分组的名字用字池：仙侠/武侠/现代都市/西幻/日式，外文名库单列），用户可在设置中维护"自定义用字池"（存 userDb config，JSON 列）；② **生成规则**——风格 × 性别 × 单双字 × 避开本书已用名（对 novel_entities 去重）四维过滤，结果一次给 10 个，支持换一批与收藏；③ **出口**——右栏新增"工具"Tab 承载（详见 R31），名字可**一键插入正文光标处 / 一键建为角色卡（名称带入）**；④ **收藏夹**——按作品持久化。**Non-goal**：不做基于 LLM 的起名（归 R17 独立立项） |
| R19 | 文件备份 | 一键全库导出 zip / 定时备份到本地目录 |
| **R21** | **人物出场章节统计**（v0.3） | 基于白名单匹配统计各章出场，资料卡显示"最近出场/出场章节列表"，辅助伏笔管理 |
| **R22** | **未标记人名启发式推荐**（v0.3） | 高频未登记词作为"可能是角色？"候选提示（纯频率统计，非 NLP；只提示不自动标注，用户确认后才入卡） |
| **R26** | **自定义等级转换关系**（v0.4） | 跨体系等级映射由用户自定义（如"武者九品 ↔ 修士炼气期"标记为等同/对应，或 A 境界"晋升为" B 境界，或自定义文本）：数据表 M1 落好（novel_level_conversions），对照表编辑 UI 放 M3 |
| **R27** | **一键生成人物关系网络**（v0.4 提出，v0.5 升级为明确框架） | **数据零新增**：完全复用 R24 的 `novel_links`（要素两两关联）+ `novel_entities`，一键渲染即可，这是 R24 数据结构的直接红利。**框架**：① **渲染**——SVG 力导向图（节点 = 要素，边 = 关联），自研轻量力导向或引入 d3-force（仅此一个依赖，渲染层局部使用不污染全局）；② **交互**——节点点击直达要素详情、节点拖拽固定、按类型/关系筛选（只看角色、只看敌对线）、双击边可加备注；③ **布局记忆**——节点拖定后坐标存 localStorage（导出 PNG 是纯渲染行为，不落库）；④ **入口**——右栏"要素"Tab 顶部"关系网络"按钮切换全宽视图；⑤ **降级**——要素 < 3 或关联 < 2 时提示"先建立关联"。**验收**：200 要素 + 500 关联下首次布局 < 1s，拖拽不掉帧 |
| **R28** | **地图创建**（v0.5 新增） | **定位**：架空世界示意图编辑器——网文作者画"势力分布图 / 大陆格局图"的轻量工具。**Non-goal（写死）**：不做真实地理地图/测绘数据/在线瓦片（规避地图数据合规与体量问题），只做作者手绘架空世界。**框架**：① **数据**——新表 `novel_maps(id, work_id, name, content JSON, created_at, updated_at)`，content 存画布文档（节点/连线/标注/视口）；节点可**绑定 location 类型要素**（改地名双向同步）；② **画布**——SVG 编辑器：网格底、节点（城池/山脉/河流/关卡四类图形 + 自由标签）、节点间连线（可标距离/险要度）、缩放平移、撤销重做（复用编辑器 same 心智）；③ **出口**——导出 PNG（存本地目录，走主进程文件 IPC）、插入灵感速记；④ **入口**——右栏"工具"Tab；⑤ **分期**——M3 先做"节点 + 连线 + 导出"最小闭环，河流曲线/地形笔刷等放迭代 |
| **R30** | **伏笔管理与章节梗概**（v0.5 补录：代码已实现，此前文档缺位） | ① 伏笔条目（`novel_outline_entries` 表）：标题/备注/待回收-已回收状态，可挂到具体章节或卷级；大纲板骨架由真实卷章派生，伏笔单独落库；② 灵感速记**一键转伏笔**（转出后卡片标记归属）；③ 章节梗概（`novel_chapters.outline_note`）：一句话剧情行内编辑；④ 灵感支持置顶。以上均已在代码中可用，本条目为文档追认 |
| **R31** | **右栏「工具」Tab**（v0.5 新增） | 承载 R18 起名工具与 R28 地图入口的第四类面板形态：右栏 Tab 在「大纲 ｜ 要素 ｜ 灵感 ｜ 检索」之外扩展「工具」，Tab 内以子卡片（起名器 / 地图列表）组织，避免顶栏按钮 proliferation；工具卡片即开即用、关即释放，不常驻内存 |

---

## 6.5 实现现状盘点（v0.7 · 2026-09-21 对照代码）

> 盘点基准：`electron/handlers/novel-handlers.ts`、`electron/db.ts`、`src/windows/WorkerWindow/pages/NovelPage/`（NovelPage + 25 个私有组件 + 6 个 hooks + novel-editor.ts 标注层 + novel-service.ts）。**当前产品形态：WorkerWindow 打开即进入编辑器（无书架页），首次使用自动播种"未命名作品"实现零配置开写，作品切换在顶栏下拉。**

### 6.5.1 已实现（全链路可用）

| 需求 | 实现落点（代码证据） |
|------|-------------------|
| R2 编辑器内核 | CodeMirror 6 已落地；标注层 IME 组合期冻结、文档变化 300ms 防抖、仅扫可视区 ± 一屏、空事务刷新不污染撤销栈（novel-editor.ts） |
| R3 防丢稿三保险 | 800ms 防抖 + 30s 强制 flush；增量快照双阈值（500 字 / 5 分钟）同事务落库、每章环形保留 20 版；会话标记法崩溃恢复 + RestoreBanner 一键还原（novel-handlers.ts `buildRecovery`/`novel-chapter-save`） |
| R4 实时统计 | 状态条常驻：本章字数 / 今日新增 / 码字速度 / 目标进度条；字数口径（含标点 / 纯汉字）可切换（StatusBar + SettingsDrawer） |
| R7 支撑面板 | 右栏四合一 Tab 已上线：大纲（卷章骨架派生 + 伏笔）/ 要素 / 灵感 / 全库检索（SupportPanel + 各 Panel 组件，含单测） |
| R8 打字机 + 专注 | 打字机模式顶栏开关；F11 专注模式（左右栏强制收起，is-focus 类） |
| R9 分卷管理 | 卷新建/重命名/拖拽重排；章节跨卷移动 + 拖拽重排，带**防误触确认弹窗**（ReorderConfirmModal，预览序号变更） |
| R20 全链路 | ① 词库高亮（名称+别名白名单、按类型开关、compartment 热更新）；② hover 悬浮资料卡（200ms 出 / 100ms 消，无焦点）；③ 选区右键"标记为角色 / 关联为别名"（EntityContextMenu） |
| R21 出场章节统计 | 资料卡展示出场章节列表（可跳转、序号标签派生），getAppearances 全量白名单匹配（NovelPage.tsx） |
| R23 自定义类型（v0.7 步骤二收口） | 六类预置 + JSON 自定义键值字段 + **用户自建类型**：定义存 config 键 `novel_entity_types`（整读整写 + `sanitizeCustomTypes` 校验），id 固定 `ct-` 前缀；EntityTypeManager 弹框管理（命名 / 8 色板选色 / 各类型张数统计 / 删除时要素迁移回「自定义」）；EntityTypesProvider Context 运行期派生 meta（自建弱色 `color-mix` ），六组件经 `useEntityTypeMeta()` 查 meta；CodeMirror 标注层对自建类型 Decoration.mark 内联注入 `--nv-mark-color` |
| R24 要素关联 | novel_links 多态关联：增/删关联、关系类型自定义、双向展示（EntityDetail 关系列表） |
| R25 等级体系管理（v0.7 步骤二收口） | `novel-level-system-add/rename/delete` + `novel-level-add/rename/delete/order` 六 IPC（主进程事务 + 渲染层乐观同步）；LevelSystemManager 弹框（体系增删改、等级项增删改/上下移）；EntityDetail「当前境界」区块：体系下拉 + 境界点选绑定，落一条 `novel_links` 行（`toType:"level"` + `relation:"当前境界"`，一要素仅一条，先摘旧再落新），删除体系/等级项级联摘除绑定；模板预置沈孤舟→凝丹境示例 |
| **R13 TXT 导出（v0.7 步骤二）** | 作品菜单「导出整本/本卷/本章」；渲染层纯函数组装（`buildBookPlainText`/`buildVolumePlainText`/`buildChapterPlainText`/`buildEntityCardText`），主进程仅 saveDialog + 写文件；章节标题按 numberStyle+后缀派生（`第一章 灵潮起`/`第12回 xxx`，章序号全书连续，与界面一致）、卷标题对齐 volumeDisplayName；EntityDetail「导出设定卡」由 toast 占位做实 |
| **R14 usage_log 埋点（v0.7 步骤二）** | `novel_usage_log` 表 + `novel-usage-log`/`novel-usage-today` IPC；editor_open / chapter_save / snapshot / goal_reach 四事件；「今日累计」改为启动时 SUM(delta) 净增口径 + 会话内续加，替换会话级内存过渡方案——北极星指标自动观测的前提 |
| R30 伏笔与梗概 | 全部已实现（详见需求表，此处为追认记录） |
| **R5 排版设置持久化（v0.6 步骤一）** | `novel-config-get/set` IPC 落 userDb config 表；`mergeEditorSettings` 纯函数逐字段校验（类型守卫 + 区间夹取 + annotationTypes 过滤去重），损坏数据安全回退默认值；启动恢复 + 变更即写回（novel-utils.ts / novel-handlers.ts） |
| **R6 续写位置记忆（v0.6 步骤一）** | 「作品/章节/光标偏移/滚动位置」四元组 500ms 防抖落库 + 关窗 flush；启动 `sanitizeRestorePosition` 校验归属后，EditorPane 下一帧原位恢复光标与滚动 |
| **R10 章节内查找替换（v0.6 步骤一）** | `@codemirror/search` 接入，Ctrl+F / Ctrl+H，面板主题化；全书检索（右栏）与章节内查找双入口并存 |
| **R29 作品管理（v0.6 步骤一）** | `novel-work-add/rename/delete` 三 IPC（删除单事务级联清理全部关联数据）；顶栏 WorkManageMenu（新建/重命名/删除确认）；作品下拉展示章节数与字数 |
| 章节删除（v0.6 补充） | 章节行悬浮删除按钮 + Popconfirm 确认；`novel-chapter-delete` 与历史快照同事务清理；删当前章自动切邻居章节 |
| 卷级新建章节（v0.6 补充） | 卷头悬浮「+」按钮，向指定卷末尾新建并自动选中；底部「新章节」/ Ctrl+N 保留；卷内排序取 max(sort)+1 防删章后撞号 |
| 编辑器点击兜底（v0.6 补充） | 点击正文下方空白区聚焦编辑器并落光标（命中不到内容点时落文档末尾），恢复"点哪都能接着写"手感 |
| 卷默认名展示（v0.6 补充） | `volumeDisplayName` 统一口径：存储名"未命名卷"时按 sort 派生（第一卷/第2部…），自定义名展示「第N卷 · 名字」（面包屑/确认弹框/卷头三处一致） |
| 快捷键 | Ctrl+S / Ctrl+P（章节模糊跳转 ChapterJumpPalette）/ Ctrl+Enter 新章 / Ctrl+F 查找 / Ctrl+H 替换 / F11 / Esc 按优先级关浮层（useNovelShortcuts，IME 期间放行，defaultPrevented 守卫防双触发） |
| 窗口级 | WorkerWindow 1200×760（min 800×560）、无 Esc 关窗、closed/应用退出双路径标记会话正常结束（main.ts） |

### 6.5.2 部分实现（有明确缺口）

> v0.6 更新：R1 作品管理、R5 设置持久化、R6 位置记忆、R10 章节内查找替换四项缺口已由**步骤一**补齐并移入 §6.5.1。
> **v0.7 更新：R23 自建类型、R25 等级管理、R13 导出三项缺口已由步骤二补齐并移入 §6.5.1——本表清零，当前无部分实现项。**

### 6.5.3 未实现（需求已立项，代码未动）

R11 自动排版 ｜ R12 全局快捷键 Alt+W + 托盘入口 + 窗口 bounds 记忆（main.ts 内有 TODO 注释）｜ R15 敏感词（标注层基建已就绪，只差词表与匹配复用）｜ R16 统计报表 ｜ R17 AI（独立立项）｜ R18 随机起名 ｜ R19 备份 ｜ R22 未标记人名推荐 ｜ R26 等级转换 UI（表已建）｜ R27 人物关系网络 ｜ R28 地图创建

> v0.6 更新：R29 作品管理已完成，从本清单移除。
> **v0.7 更新：R13 TXT 导出、R14 usage_log 埋点已完成，从本清单移除。**

### 6.5.4 代码已实现、此前文档未体现（本次补录追认）

| 能力 | 代码落点 | 处置 |
|------|---------|------|
| 伏笔系统（条目 CRUD、挂章/挂卷、待回收-已回收、卷头未回收计数） | `novel_outline_entries` 表 + OutlinePanel | 补录为 R30 |
| 章节一句话梗概（行内编辑、空串清除） | `novel_chapters.outline_note` 列 + novel-chapter-outline IPC | 补录为 R30 |
| 灵感置顶 + 一键转伏笔（转出标记 foreshadowId） | novel_notes.pinned / foreshadow_id | 补录为 R30 |
| 序号体系自定义（阿拉伯/中文数字 × 章/卷后缀自由组合） | numberStyle/chapterSuffix/volumeSuffix 设置 | 并入 R5 描述 |
| 拖拽重排防误触确认（预览序号变更再应用，默认开） | confirmReorder 设置 + ReorderConfirmModal | 并入 R9 描述 |
| 小屏自动收起左栏（舞台宽 < 560px 让位码字区） | LAYOUT.minStageWidth + useNovelViewState | 实现细节，记录备查 |
| 关联要素/灵感/大纲等单测 | OutlinePanel/InspirationPanel/novel-utils 测试 | 质量保障，记录备查 |



## 7. 工程落点（对照 AGENTS.md，窗口级改造清单）

**窗口级（electron/main.ts + WorkerWindow）：**
- `createWorkerWindow()` 默认尺寸 720×520 → **1200×760**（min 420×320 → 800×560），并持久化上次 bounds
- **移除 App.tsx 中 Esc 直接关窗逻辑**（替换为编辑器内语义：退出专注模式/无操作）
- 即关即销语义**保留**——对编辑器反而是优点：无状态腐化，数据安全完全由持久化层兜底
- 标题/图标更新为编辑器身份

**数据层（userDb 新表，SQL 只进 db.ts / handlers）：**

```text
novel_works(作品) / novel_volumes(卷, P1) / novel_chapters(title, content, word_count, status, sort, outline_note)
novel_snapshots(chapter_id, content, created_at)   -- 每章环形保留 20 版
novel_notes(灵感速记, 含 pinned/foreshadow_id) / novel_outline_entries(伏笔条目) / usage_log(埋点, 待建)
-- ↑ novel_outline_entries 与 outline_note 为 v0.5 追认：代码已建，v0.4 文档漏记

-- ── 要素模型（v0.4：原 novel_cards 升级，类型可扩展 + 互相关联 + 等级体系）──
novel_entities(id, work_id, type, name, aliases, summary, content,
               fields, sort, created_at, updated_at)
    type: character/location/faction/item/level_system/custom…（用户可自建类型）
    fields: 自定义键值扩展字段，JSON 列承载（SQLite JSON1 可查询）——预留扩展性，避免 EAV 过度设计
novel_links(id, from_type, from_id, to_type, to_id, relation, note)
    -- 多态关联：任意要素两两互相关联（人名↔地名、角色↔势力、角色↔等级…）
    -- relation 预置常用值（师徒/亲属/敌对/所属/出身地/当前境界）+ 用户自定义文本
novel_level_systems(id, work_id, name, note)         -- 等级体系（如"修真境界"）
novel_levels(id, system_id, name, rank, note)        -- 等级项，rank 即顺序（练气→筑基→金丹…）
novel_level_conversions(id, from_level_id, to_level_id, relation, note)
    -- 用户自定义等级转换关系：等同/对应/晋升为/自定义文本
    -- 表结构 M1 一次到位，编辑 UI 按 R26 分期（M3）

-- ── v0.5 新增 ──
novel_maps(id, work_id, name, content, created_at, updated_at)   -- R28 地图文档
    content: JSON 画布文档（节点/连线/标注/视口），节点可绑定 location 要素 id
-- 起名词库（R18）不建表：内置词库为静态资源随应用分发，
-- 用户自定义用字池与收藏夹存 userDb config（JSON 值），避免为低频小数据建表
```

- 新建 `electron/handlers/novel-handlers.ts`（语义化 IPC：`novel-chapter-save` 等）；保存 + 快照写入同事务
- 字数在保存时计算并落列；**词库（要素名称+别名）随要素保存时同步刷新到渲染层内存**，供标注层匹配
- 新依赖建议：CodeMirror 6（纯文本模式）——虚拟滚动、性能与 IME 支持成熟，可深度定制打字机/护眼主题；备选受控 textarea（最简但打字机模式/标注层难做）；**排除** Monaco/ProseMirror。注意 pnpm 11 新依赖有发布冷却期（minimumReleaseAge）

**R20 技术要点**：CodeMirror 6 增量 decoration（仅可视区 + 防抖）承载人名高亮；`hoverTooltip` 扩展实现资料卡（无焦点、composition/输入期间强制关闭）；与敏感词（R15）共用同一标注管线。**这套能力反过来印证了内核选 CodeMirror 6 的必要性。**

**边界合规**：编辑器组件留在 `WorkerWindow/` 私有目录（文件夹化组件、index.scss）；仅单使用方不上移 shared；窗口隔离不 import BaseWindow。纯增量，不触碰 Todo/Memo/Daily 链路。

## 8. 成功指标

> M1 无埋点阶段以**手动验收清单**替代；M2 上 R14 后转自动统计。

| 层级 | 指标 | 基线 | 目标 | 观测 |
|------|------|------|------|------|
| 北极星 | 周有效码字天数（当日新增 ≥500 字记 1 天） | 0 | 上线 1 个月 ≥ 4 天/周 | usage_log |
| 驱动 | 打开→10 分钟内开始输入的转化率 | — | ≥ 80% | usage_log |
| 驱动 | 单次码字时长中位数 | — | ≥ 25 min | usage_log |
| 驱动 | 今日目标达成率 | — | ≥ 40% | usage_log |
| 驱动 | 资料卡周使用次数（hover 查看 / 标记为角色） | — | 设定卡用户人均 ≥ 10 次/周 | usage_log |
| 健康 | 输入延迟 P95 | — | < 50ms | 性能抽样日志 |
| 健康 | 标注层开启后的输入延迟劣化 | — | ≤ 2ms | 开/关标注层对照测试 |
| 健康 | 丢稿事件 / 快照回滚失败 | — | = 0 | 演练清单 |
| 健康 | 窗口冷启动 P95 | ~1s | ≤ 1s | 手动抽测 |

## 9. 里程碑

| 阶段 | 范围 | 出口标准 |
|------|------|---------|
| **M1 · 编辑器核心** | R1–R6（P0 全部）——**✅ 已完成（v0.6）**：作品 CRUD、设置持久化、位置记忆、章节内查找替换均已落地（v1.9.x），M1 范围无剩余缺口。待人工演练：① 断电/杀进程/连点关闭防丢稿；② 搜狗+微软拼音连续输入 30 分钟零异常；③ 连写 30 分钟无卡顿；④ 冷启动 ≤1s |
| **M2 · 创作支撑** | R7–R14、R20、R23–R25、R29、R31——**R7/R8/R9/R10/R13/R14/R20/R23/R24/R25/R29 已完成（v0.7）**，M2 功能面收口；仅 R31（工具 Tab）随步骤三起名工具落地 | 右栏面板可用（要素库含自定义类型/字段/关联/等级体系基础管理）；人名高亮 + 悬浮资料卡全链路可用且标注层性能达标；全书检索 <200ms；北极星可自动观测 ✅（R14 埋点已就位） |
| **M3 · 专业进阶** | R11、R15–R16、R19、R21–R22、R26–R28 + AI 立项评估 | 按 M1/M2 数据决策 |

### 分步实施路线（v0.5 · 按盘点重排，可直接当开发排期用）

> 原则：先补 M1/M2 的**硬缺口**（数据安全与多作品基础），再做**零新表的小工具**（起名），再吃 R24 数据红利（关系网络），最后做**重交互**的地图；每步独立可交付、可验收。

**步骤一 · 补齐 M1 收尾（编辑器地基，最高优先）—— ✅ 已完成（2026-09-20，应用 v1.9.0–v1.9.2）**
1. ✅ 设置持久化（R5 缺口）：settings 落 userDb config 表，`mergeEditorSettings` 纯函数校验 + 启动恢复；
2. ✅ 续写位置记忆（R6 缺口）：四元组（作品/章节/光标/滚动）防抖落库 + 关窗 flush，重开原位恢复；
3. ✅ 作品 CRUD（R29 / R1 缺口）：novel-work-add/rename/delete 三 IPC（删除单事务级联清理）+ 顶栏 WorkManageMenu 管理入口，下拉展示章节数/字数；
4. ✅ 章节内查找替换（R10 缺口）：Ctrl+F/H，@codemirror/search 主题化面板。
> 附带体验修复（v1.9.1/v1.9.2）：章节删除（级联快照 + 悬浮按钮）、卷级「+」新建章节入口、点击正文下方空白聚焦续写、未命名卷展示「第N卷」。
> 待人工演练：M1 出口标准四项（防丢稿演练 / IME 30 分钟 / 长写性能 / 冷启动）。

**步骤二 · M2 剩余面板能力 —— ✅ 已完成（2026-09-21，应用 v1.9.3）**
5. ✅ 等级体系管理（R25 缺口）：体系/等级项 CRUD IPC + EntityDetail 里角色关联"当前境界"（复用 novel_links，`toType:"level"` + `relation:"当前境界"`，一要素仅一条绑定）；
6. ✅ TXT 导出（R13）：整本/按卷/按章导出，主进程文件 IPC；EntityDetail"导出设定卡"占位做实；**章节标题按序号设置派生（numberStyle + 章/卷后缀），与界面所见一致**（用户硬性要求）；
7. ✅ usage_log 埋点（R14）：唤起/保存/快照/目标达成四事件，今日统计改埋点净增口径——北极星指标自动观测就绪；
8. ✅ 自定义类型管理（R23 缺口）：用户建类型（命名/8 色板），定义存 config + Context 运行期派生 meta，标注层内联注色；删除类型时要素迁移回「自定义」。

**步骤三 · 起名工具（R18 + R31，零新表，可独立发版）—— ⬅ 当前待办**
9. 右栏"工具"Tab 骨架（R31）；
10. 内置词库（姓氏池 + 五风格用字池 + 外文名库，静态 JSON 资源）；
11. 生成器（四维过滤 + 换一批 + 收藏）+ 双出口（插入正文 / 建角色卡）。

**步骤四 · 人物关系网络一键生成（R27，数据零新增）**
12. 力导向渲染（自研或 d3-force，SVG）：nodes=novel_entities，edges=novel_links；
13. 交互三件套：节点点击→详情、拖拽固定（坐标存 localStorage）、类型/关系筛选；
14. 边备注与导出 PNG；性能验收（200 节点 + 500 边 < 1s 首布局）。

**步骤五 · 地图创建（R28，重交互，放最后）**
15. novel_maps 表 + 全量装载/保存 IPC；
16. SVG 画布最小闭环：网格 + 节点（四类图形）+ 连线（标距离）+ 缩放平移 + 撤销重做；
17. location 要素绑定（双向同步地名）+ 导出 PNG + 插入灵感；
18. 迭代项：河流曲线、地形笔刷、多图层。

**持续池（有数据再决策）**：R11 自动排版（可提前插队，纯文本变换工作量小）、R15 敏感词（复用标注层）、R16 统计报表（依赖步骤 7）、R19 备份、R22 人名推荐、R26 等级转换 UI、书架完整页、AI 立项。

## 10. 风险与开放问题

**风险**

| 风险 | 等级 | 对策 |
|------|------|------|
| IME 兼容缺陷直接摧毁码字体验 | 高 | M1 第一周即做内核选型 time-box 验证（2 天），拼音实测不过则换 textarea 方案 |
| 富文本需求诱惑导致范围蔓延 | 高 | Non-goal 写死；正文永远纯文本段落流 |
| 标注层（高亮/hover 卡）拖慢输入 | 中 | 白名单匹配 + 可视区增量 + 防抖三重节流；"劣化 ≤2ms"硬验收；提供全局开关一键关闭标注 |
| 快照策略不当（过频卡顿/过疏丢稿） | 中 | 增量快照节流（5min/500 字双阈值）+ M1 演练校准 |
| Esc/误触关窗心理不适 | 中 | 已移除 Esc 关窗；关窗永不丢数据是硬验收 |
| 地图编辑器滑向"绘图软件"（范围蔓延新源头，v0.5） | 高 | R28 写死最小闭环（节点+连线+导出）+ Non-goal 排除真实地理数据；地形笔刷类功能需使用数据投票才立项 |
| 关系网络在超图（200+ 节点）下交互掉帧 | 中 | 力导向仅作为初始布局、稳态后停止模拟；节点聚合（按势力折叠）作 P2 兜底 |

**开放问题**

1. **编辑器内核**：CodeMirror 6（推荐）vs 受控 textarea —— 工程侧 2 天验证后拍板（阻塞 R2/R20，不阻塞其他开发）
2. **载体确认**：仍按"改造 WorkerWindow"执行（本 PRD 前提）；若未来想保留通用临时窗，独立 NovelWindow 拆分成本低
3. **今日字数口径**：按净增（删字扣回，推荐）还是按保存动作累计
4. **快照保留 20 版/章**是否足够（存储估算：单章 1 万字 × 20 版 × 100 章 ≈ 20MB，无压力）
5. **同名角色处理**（v0.3）：同名不同人时 v1 按唯一卡匹配 + 资料卡内提示"存在同名卡"；是否需要"指定某卡仅在某章生效"等消歧机制 → M3 按真实反馈定
6. **要素字段模板**（v0.4）：v1 用"自定义键值字段"兜底扩展性；若 M2 数据显示用户高频重复添加同类字段，再升级为"按类型的字段模板管理器"（避免过早设计）
7. **等级转换的方向语义**（v0.4）：`novel_level_conversions` 的 relation 目前是无约束文本（等同/晋升为/自定义）；若需要"晋升链"自动推导（A→B→C 传递），届时升级为有向图校验 → M3 按需
8. **书架的形态**（v0.5）：当前"打开即编辑器 + 顶栏切换"对单作品作者是零摩擦优点；R29 先做顶栏内嵌轻量管理，**完整书架页（封面墙/分类/搜索）等出现 ≥3 部作品的重度用户再评估**，避免过早设计
9. **起名词库来源**（v0.5）：v1 内置静态库（五风格 + 外文名）；是否开放"导入词库包"（用户自带 txt/json）看真实需求
10. **地图编辑器的复杂度边界**（v0.5）：严格按"节点 + 连线 + 导出"最小闭环起步；地形笔刷 / 河流曲线 / 等高线等重功能必须有使用数据支撑才投入——地图工具的行业失败教训普遍是"做成了绘图软件"
11. **关系网络的布局记忆**（v0.5）：v1 坐标存 localStorage（本机记忆即可）；若未来出现"多设备同构图"需求再考虑落库到 novel_maps 同级的布局表

---

### 附：一页纸决策摘要

- **是什么**：本地优先、秒开、防丢稿做到极致的专业小说编辑器（WorkerWindow 改造承载；**无书架页，打开即写**，作品切换在顶栏）。
- **凭什么是我们**：数据主权（本地 SQLite）+ 与 CheckIn 生态融合（写作时段/日更目标）+ 零会员零广告 + **要素库长在正文里且万物可关联**（人名高亮/悬浮资料卡/要素互相关联/等级体系与自定义转换）。
- **不做什么**：富文本、NLP 人名识别、云同步、AI（独立立项）、强制锁机、平台对接、**真实地理地图**（R28 只做架空世界示意图）。
- **当前进度**（§6.5 / v0.7）：**M1 已完成**（作品 CRUD / 设置持久化 / 位置记忆 / 章节内查找替换全部落地，仅剩出口标准人工演练）；**M2 功能面收口**（R7–R10、R13、R14、R20、R23–R25、R29 全部落地，仅 R31 工具 Tab 随步骤三落地）；R27 关系网络的数据基础（R24 关联表）已就位，只差渲染层。
- **下一步**（§9 路线）：③ 起名工具（R18+R31，零新表）→ ④ 一键人物关系网络（R27）→ ⑤ 地图编辑器（R28）。
- **成败判据**：输入 P95 <50ms（标注层劣化 ≤2ms）、丢稿=0、打开即续写 ≤3s、周有效码字 ≥4 天。
